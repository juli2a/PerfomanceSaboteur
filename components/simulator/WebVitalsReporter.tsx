"use client";

// Mounted once in root layout
// Subscribes to web-vitals (LCP, CLS, INP) and writes values to the simulator store
// The PerformancePanel reads from the same store
export default function WebVitalsReporter() {
  return null;
}
