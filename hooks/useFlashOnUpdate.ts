"use client";

// Returns a ref + className string that briefly highlights a component border on each render
// Used by FlashOnUpdate wrapper and individual table rows
export function useFlashOnUpdate() {
  return { className: "" };
}
